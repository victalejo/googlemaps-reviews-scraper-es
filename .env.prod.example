# ============================================================================
# CONFIGURACIÓN DE PRODUCCIÓN - Google Maps Scraper API
# ============================================================================
# Copia este archivo a .env y configura con tus valores reales

# ============================================================================
# DATABASES (Usar servicios del docker-compose o externos)
# ============================================================================

# MongoDB - Usar el interno del docker-compose o uno externo
# Interno (default): mongodb://mongodb:27017/
# Externo ejemplo: mongodb://usuario:password@host:27017/
MONGODB_URL=mongodb://mongodb:27017/
MONGODB_DB=googlemaps

# Redis - Usar el interno del docker-compose o uno externo
# Interno (default): redis://redis:6379/0
# Externo ejemplo: redis://default:password@host:6379
REDIS_URL=redis://redis:6379/0

# ============================================================================
# API CONFIGURATION
# ============================================================================

# Modo headless para Selenium (siempre True en producción)
HEADLESS_MODE=True

# Nivel de logs (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============================================================================
# SCRAPING CONFIGURATION
# ============================================================================

# Intervalo de chequeo por defecto (minutos)
DEFAULT_CHECK_INTERVAL=60

# Número de reviews por defecto
DEFAULT_REVIEWS_COUNT=100

# Máximo de reviews por request
MAX_REVIEWS_PER_REQUEST=500

# ============================================================================
# WEBHOOK CONFIGURATION
# ============================================================================

# Reintentos para webhooks fallidos
WEBHOOK_MAX_RETRIES=3

# Delay entre reintentos (segundos)
WEBHOOK_RETRY_DELAY=5

# ============================================================================
# NOTAS DE DESPLIEGUE
# ============================================================================
#
# 1. Si usas MongoDB/Redis externos (como servicios gestionados):
#    - Actualiza MONGODB_URL con tu connection string
#    - Actualiza REDIS_URL con tu connection string
#    - Considera comentar los servicios mongodb y redis en docker-compose.prod.yml
#
# 2. Si usas MongoDB/Redis del docker-compose (incluidos):
#    - Deja los valores por defecto
#    - Asegúrate que los puertos 27017 y 6379 estén disponibles
#    - Los datos se guardan en volumes persistentes
#
# 3. Para producción se recomienda:
#    - Usar servicios gestionados de MongoDB (Atlas, etc)
#    - Usar servicios gestionados de Redis (Redis Cloud, etc)
#    - Configurar backups automáticos
#    - Configurar monitoreo y alertas
#
